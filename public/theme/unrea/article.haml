%article.article
  %section.entry
    %h2.entry_title
      = link_to entry.title, entry.link
    %section.entry_body
      %img.gravatar{:src => gravatar_image_url(entry.user.email)}
      = entry.body
  %footer.entry_footer
    .entry_info    
      %span.link_to_entry_comment
        = link_to "#{t.comments}(#{entry.comments.size})", "#{entry.link}#comment_wrapper"
      %span.entry_date
        = entry.updated_at.strftime("%Y/%m/%d %H:%M:%S") 
    .entry_social
      - absolute_url = URI.join("http://unrea.info/", entry.link).to_s
      .entry_tweet
        - unless request.secure?
          = tweet_button(absolute_url, "#{entry.title} - #{@site.title}")
      .entry_hatena
        - unless request.secure?
          = hatena_bookmark_button("#{entry.title} - #{@site.title}", absolute_url)
      .entry_mixi
        - unless request.secure?
          = mixi_check(absolute_url)
      .entry_gree
        - unless request.secure?
          = gree_social_feedback(absolute_url)
      .entry_plusone
        = plusone(absolute_url)
      .entry_facebook
        - unless request.secure?
          = facebook_like_button(absolute_url)



